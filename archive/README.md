# archive/

過去のスクリプト・ノートブックの退避先。削除はしていないので、必要になればここから取り出せます。

退避日: 2026-05-14

## 中身

### `scripts/`
- `feasibility_2d_polarity.py`, `feasibility_2d_validate.py`, `feasibility_k3.py`
  データ構造の初期探索用スクリプト。検証は完了済み。

### `notebooks/`
- `01_exploration.ipynb`, `02_results.ipynb`
  中身がほぼ空の stub。
- `colab_full_run.ipynb`, `colab_full_run_unzipped.ipynb`
  フルパイプライン用 Colab ノートブックの旧版。後継の `notebooks/colab_full_run_v2.ipynb` で機能的に置換済み(M0/M1/M2 モデル比較含む)。

## 補足

git 履歴は `git log --follow archive/<path>` で追えます。
